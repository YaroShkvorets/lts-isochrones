"use strict";
/**
 * Pick
 */
function pick(object, keys) {
    const container = {};
    Object.keys(object).map(key => {
        if (keys.indexOf(key) !== -1) {
            container[key] = object[key];
        }
    });
    return container;
}
exports.pick = pick;
